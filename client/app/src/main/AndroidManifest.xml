<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.caocao.client">

    <application
        android:name=".base.app.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:resizeableActivity="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.demand.DemandActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.serve.ServeGenreActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.serve.SkillActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan|stateHidden" />

        <activity
            android:name=".ui.serve.AddSpecActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.serve.SkillReleaseActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>